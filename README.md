# llm-support-ticket-auto-tagging-advanced-phase2-
AI-powered support ticket auto-tagging system using zero-shot and few-shot learning with a transformer-based LLM (BART). The model classifies customer queries into multiple categories and outputs top-3 predicted tags with visualization and dataset-based evaluation.
